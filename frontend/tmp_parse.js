const fs = require('fs'); const parser = require('@babel/parser'); const lines = fs.readFileSync('src/components/PanelCoordinacion.js', 'utf8').split(/\r?\n/); let lo = 0, hi = lines.length; while (lo < hi) { const mid = Math.floor((lo + hi) / 2); const snippet = lines.slice(0, mid).join('\n'); try { parser.parse(snippet, { sourceType: 'module', plugins: ['jsx'] }); lo = mid + 1; } catch (e) { hi = mid; } } const from = Math.max(0, hi - 8); const to = Math.min(lines.length, hi + 8); console.log('fail ' + hi); for (let i = from; i < to; i++) { console.log(${i+1}: ); }