from django.db import migrations


class Migration(migrations.Migration):
    dependencies = [('preregistro', '0012_entrevistavirtual')]

    operations = [migrations.RunSQL(
        sql='''
        CREATE TABLE IF NOT EXISTS preregistro_examenenlinea (
            id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            tipo varchar(20) NOT NULL,
            titulo varchar(255) NOT NULL DEFAULT 'Examen en línea',
            descripcion text NOT NULL DEFAULT '',
            business_key varchar(120) NOT NULL DEFAULT '',
            camunda_instance_id varchar(80) NOT NULL DEFAULT '',
            fecha_programada timestamptz NULL,
            fecha_inicio timestamptz NULL,
            fecha_fin timestamptz NULL,
            duracion_minutos integer NOT NULL DEFAULT 90,
            estado varchar(20) NOT NULL DEFAULT 'programado',
            calificacion numeric(4,2) NULL,
            intentos integer NOT NULL DEFAULT 0,
            max_intentos integer NOT NULL DEFAULT 3,
            retroalimentacion text NOT NULL DEFAULT '',
            created_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
            updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
            aspirante_id bigint NOT NULL REFERENCES preregistro_aspirante(id) ON DELETE CASCADE
        );
        CREATE UNIQUE INDEX IF NOT EXISTS preregistro_examenenlinea_business_key_uniq ON preregistro_examenenlinea (business_key) WHERE business_key <> '';
        CREATE INDEX IF NOT EXISTS preregistro_examenenlinea_aspirante_idx ON preregistro_examenenlinea (aspirante_id);
        ''',
        reverse_sql=migrations.RunSQL.noop,
    )]
